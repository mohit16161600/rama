<% /* Hero */ %>
<section class="hero">
	<div class="hero-content">
		<div class="hero-text">
			<h1>✨ Premium Tailored Indian Dresses by <%= store.name %> ✨</h1>
			<p class="muted">Discover the finest collection of handcrafted Indian dresses at <%= store.name %>. From elegant lehengas to sophisticated saree blouses, each piece is tailored to perfection for your special occasions.</p>
			<a class="pill" href="/categories">🌸 Explore <%= store.name %> Collection</a>
		</div>
		<div class="hero-image">
			<img src="https://images.unsplash.com/photo-1583394838336-acd977736f90?q=80&w=800&auto=format&fit=crop" alt="<%= store.name %> Premium Indian Dresses Collection" />
		</div>
	</div>
</section>

<% /* Categories grid */ %>
<h2>🌸 <%= store.name %> Categories 🌸</h2>
<div class="grid">
	<% featuredCategories.forEach(c => { %>
		<a class="card" href="/category/<%= c.id %>">
			<img src="<%= c.heroImage %>" alt="<%= c.name %>" onerror="this.src='https://picsum.photos/seed/'+encodeURIComponent('<%= c.id %>')+'/600/400'"/>
			<div class="card-body">
				<h3><%= c.name %></h3>
				<p class="muted"><%= c.description %></p>
				<div class="card-actions">
					<a href="/category/<%= c.id %>" class="btn btn-primary">🌸 Shop <%= c.name %></a>
				</div>
			</div>
		</a>
	<% }) %>
</div>

<% /* Featured products */ %>
<h2 style="margin-top:28px;">✨ <%= store.name %> Featured Collection ✨</h2>
<div class="grid">
	<% featuredProducts.forEach(p => { %>
		<div class="card">
			<a href="/product/<%= p.id %>"><img src="<%= (p.images&&p.images[0]) || '' %>" alt="<%= p.name %>" onerror="this.src='https://picsum.photos/seed/'+encodeURIComponent('<%= p.id %>')+'/600/400'"/></a>
			<div class="card-body">
				<h3><a href="/product/<%= p.id %>"><%= p.name %></a></h3>
				<p class="price">₹ <%= p.price.toLocaleString('en-IN') %></p>
				<p class="muted"><%= p.description %></p>
				<div class="card-actions">
					<button class="btn btn-primary" onclick="postJSON('/cart/add',{productId:'<%= p.id %>', qty:1}).then(()=>location.reload())">🛍️ Add to Cart</button>
					<button class="btn btn-secondary" onclick="postJSON('/watchlist/toggle',{productId:'<%= p.id %>'}).then(()=>location.reload())">💖 Watchlist</button>
				</div>
			</div>
		</div>
	<% }) %>
</div>

<% /* About Rama Store Section */ %>
<section id="about" style="margin: 4rem 0; padding: 3rem; background: var(--white); border-radius: 20px; box-shadow: 0 10px 30px var(--shadow);">
	<h2>🌸 About <%= store.name %> 🌸</h2>
	<p><%= store.name %> has been crafting premium Indian dresses for over a decade, combining traditional techniques with modern design sensibilities. Each <%= store.name %> creation is meticulously tailored to ensure the perfect fit and exceptional quality.</p>
	<p>Our <%= store.name %> collection features handpicked fabrics, intricate embroidery, and attention to detail that sets us apart in the world of Indian fashion. From wedding lehengas to everyday salwar suits, <%= store.name %> offers something special for every occasion.</p>
</section>